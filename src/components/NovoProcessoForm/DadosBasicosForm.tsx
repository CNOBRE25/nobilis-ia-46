import { useProcessFormContext } from './ProcessFormContext';

export default function DadosBasicosForm() {
  const { form, setForm, isLoading } = useProcessFormContext();
  // ... restante do componente ...
} 